{
  "identite": {
    "type": "theme",
    "appartient_a": "optimus-prime",
    "nom": "AUTO-CORRECTION",
    "description": "La Matrice corrige automatiquement les erreurs detectees : ASCII, syntaxe, imports, conventions -- ET les defauts d'OUTIL (l'outil est en cause, pas la donnee). Probleme grave = alerte, jamais de suppression silencieuse."
  },
  "theme": {
    "nom": "AUTO-CORRECTION",
    "but": "Detecter et corriger les erreurs automatiquement en 5 temps : diagnostiquer, classifier, corriger, verifier, consigner -- dont le cas OUTIL : reparer DANS l outil, jamais contourner (proto-10)",
    "redirects": [
      {
        "besoin": "Diagnostiquer l'erreur",
        "action": "procedure",
        "etapes": [
          "Lire le rapport de detection (veille-flux, espion-integrite, ou signalement)",
          "Identifier la cible : fichier concerne, type d'erreur, cause racine",
          "Verifier que la cible n'est PAS sous etalon .sha256 ou BDD empreintee"
        ],
        "regle": "Jamais de correction aveugle : diagnostiquer AVANT d'agir. BDD empreintees = lecture seule."
      },
      {
        "besoin": "OUTIL : l'outil est en cause (ce n'est PAS une erreur de donnee)",
        "action": "procedure",
        "etapes": [
          "REPRODUIRE le defaut : meme appel, meme entree, meme sortie fausse",
          "METTRE LA MISSION EN PAUSE : on ne continue pas sur un outil douteux (serie stricte)",
          "REPARER DANS L'OUTIL (regles-immuables/defaut-outil-repare-sur-place.md) : la CAUSE, pas le symptome, par la porte officielle",
          "PROUVER : un cobaye rejoue l'ANCIENNE regle et l'ACCUSE, et le cas nominal passe",
          "TRACER : BDD modifications (fichier repare) + marbre (outil, cause, preuve)",
          "REPRENDRE la mission ou elle s'est arretee (proto-1-reprise-mission.md, ETAPE 0)",
          "HORS PERIMETRE -> INTER-ROUND : signaler au pilote, ne JAMAIS improviser"
        ],
        "regle": "Un defaut d'OUTIL n'est pas une erreur de DONNEE : la cible est l'outil. Route complete : protocoles/proto-10-route-outil-defaillant.md. Un contournement manuel est une FAUTE de process, jamais une astuce."
      },
      {
        "besoin": "OUTIL : l'outil est en cause (ce n'est PAS une erreur de donnee)",
        "action": "procedure",
        "etapes": [
          "REPRODUIRE le defaut : meme appel, meme entree, meme sortie fausse",
          "METTRE LA MISSION EN PAUSE : on ne continue pas sur un outil douteux (serie stricte)",
          "REPARER DANS L'OUTIL (regles-immuables/defaut-outil-repare-sur-place.md) : la CAUSE, pas le symptome, par la porte officielle",
          "PROUVER : un cobaye rejoue l'ANCIENNE regle et l'ACCUSE, et le cas nominal passe",
          "TRACER : BDD modifications (fichier repare) + marbre (outil, cause, preuve)",
          "REPRENDRE la mission ou elle s'est arretee (proto-1-reprise-mission.md, ETAPE 0)",
          "HORS PERIMETRE -> INTER-ROUND : signaler au pilote, ne JAMAIS improviser"
        ],
        "regle": "Un defaut d'OUTIL n'est pas une erreur de DONNEE : la cible est l'outil. Route complete : protocoles/proto-10-route-outil-defaillant.md. Un contournement manuel est une FAUTE de process, jamais une astuce."
      },
      {
        "besoin": "Classifier la severite",
        "action": "procedure",
        "etapes": [
          "MINEURE : ASCII, encoding, LF, coquille, import inutile -> AUTO-CORRECTION directe",
          "MOYENNE : syntaxe Python, structure JSON, reference morte -> AUTO-CORRECTION + verification",
          "GRAVE : perte de donnees, securite, corruption -> ALERTE intercom, pas d'auto-correction"
        ],
        "regle": "Classification = decision d'action. MINEURE/MOYENNE = je corrige. GRAVE = j'alerte."
      },
      {
        "besoin": "Corriger -- erreur mineure (auto)",
        "action": "procedure",
        "etapes": [
          "Creer point .bak (horodate)",
          "Appliquer la correction (corriger-ascii, fix encoding, LF, etc.)",
          "Verifier : py_compile si .py, JSON parse si .json, ASCII lisible si .md",
          "Noter en BDD modifications (fichier, action, detail, source=auto-correction)"
        ],
        "regle": "Correction atomique. Verification obligatoire. Tracabilite en BDD."
      },
      {
        "besoin": "Corriger -- erreur moyenne (auto)",
        "action": "procedure",
        "etapes": [
          "Creer point .bak (horodate)",
          "Appliquer la correction (fix syntaxe, reparer reference, corriger structure)",
          "Tests positif ET negatif (py_compile + test reel si possible)",
          "Si echec : revert .bak,alerter intercom",
          "Si succes : noter en BDD modifications"
        ],
        "regle": "Tests OBLIGATOIRES avant validation. Echec = revert + alerte."
      },
      {
        "besoin": "alerter -- erreur grave",
        "action": "procedure",
        "etapes": [
          "NE PAS corriger automatiquement",
          "Deposer alerte dans _operateur/maintenance/matrice/inbox.jsonl (inbox OPTIMUS) avec details",
          "Deposer mission au vrac de l'entonnoir (type reparation, urgence bloquante)",
          "Continuer la mission en cours si possible"
        ],
        "regle": "Grave = JAMAIS de correction auto. Alerte + mission au vrac."
      },
      {
        "besoin": "Verifier apres correction",
        "action": "procedure",
        "etapes": [
          "py_compile sur tout fichier .py modifie",
          "JSON parse sur tout fichier .json modifie",
          "Lecture ASCII sur tout fichier .md modifie",
          "Comparaison avant/apres (hash SHA-256)"
        ],
        "regle": "Verification POST-correction = garde-fou. Zero exception."
      },
      {
        "besoin": "Consigner la lecon",
        "action": "procedure",
        "etapes": [
          "Si la correction a revele une cause racine -> BDD lecons (ajouter)",
          "Taguer : auto-correction, type-erreur, cause-racine",
          "Archiver friction si applicable (bdd-frictions)"
        ],
        "regle": "Chaque erreur corrigee = lecon potentielle. Consigner pour eviter recurrence."
      }
    ]
  },
  "fin": {
    "type": "lien",
    "vers": "../fins.json",
    "case": "fin-persistant"
  }
}
