### definition d'un outil:
    un outil peut etre le clone d'un outil native (llm) qui sera modifiable et configurable
    il sera utiliser seul(rarement, preferer les combos qui traiteront le fichier mais la suite du combo permettra de tres rapidement effectuer une suite de test ou autre sur le fichier).